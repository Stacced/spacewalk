import { View, Text } from 'react-native';
import styled from 'styled-components/native';

const Details = props => {
    return (
        <View>
            <Text>Details !</Text>
        </View>
    )
}

export default Details;